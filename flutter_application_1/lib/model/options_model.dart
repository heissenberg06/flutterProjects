class Options {
  String title;

  Options({required this.title});

}

  List<Options> optionsList = [
    Options(title: '/okuduklarim'),
    Options(title: '/okuyacaklarim'),
    Options(title: 'title')
  ];
//bu modeli de navigation icin yaptim